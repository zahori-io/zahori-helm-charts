apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zahori-postgresql-chart.fullname" . }}-test-connection"
  labels:
    {{- include "zahori-postgresql-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zahori-postgresql-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
